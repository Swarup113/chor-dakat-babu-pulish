import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chordakat.pulish',
  appName: 'Chor Dakat Babu Pulish',
  webDir: 'dist'
};

export default config;
